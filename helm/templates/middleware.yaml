---
{{- $appName := include "app.name" . }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-{{ $appName }}-api
  namespace: {{ .Release.Namespace }}
spec:
  stripPrefix:
    prefixes:
      - /{{ $appName }}-api
